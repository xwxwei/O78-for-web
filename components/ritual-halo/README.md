# Ritual Halo

The homepage center halo lives here so its visual system can evolve independently from page layout.

- `RitualHalo.tsx`: ring sizing, stroke widths, positioning, and future motion hooks.
- `index.ts`: public export for page imports.
